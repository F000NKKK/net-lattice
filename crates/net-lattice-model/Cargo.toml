[package]
name = "net-lattice-model"
version = "0.18.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
categories.workspace = true
keywords.workspace = true
description = "The domain model of operating system networking state (routes, interfaces, DNS, ...), with no operating-system dependency."
readme = "README.md"

[dependencies]
net-lattice-core = { workspace = true }
net-lattice-ip = { workspace = true }
